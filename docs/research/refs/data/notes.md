# Data — the sweep

Driven 2026-09-22 with `tools/research/capture.ts` into five modalities (`live/`, `live2/`, `tree/`, `prov/`, `hand/`). **59 sources · 58 frames on disk · 56 distinct · 1 failed**, counted from the files; walls, redirects and identical pairs are named in `sources-index.md`. **39 Read calls** of a budget of 45: 33 new frames (the six 390 px frames tiled three-up at native size) and six stock frames. **Only a frame that was opened is cited.** Paths are relative to `docs/research/refs/data/`; stock is `C:\Users\Asaf\dev\hl-refs\ref\`.

**The job.** Show a dealer what their file holds, grouped the way they think — Stacer with its motor and trailer pairings under it, not 53 rows — with each table's count, kind and where it came from, and open any one into the sheet. Its own door on the rail ("NOT UNDER ADMIN!"). Cockpit: **18 readable rows at 1280 × 800.** The third way in — gallery, register, record — for the whole file.

## 0. Corrections, by counting the file and by looking

1. **There is no `view` table.** `data/northside/manifest.json`: 25 `base` · 28 `join` · **0 `view`** · 15,691 rows. `view` is the contract's third role (`src/domain/model/tables.ts`) and `mint.ts` rules "joins and views are never doors"; a board drawing three kinds invents one.
2. **There is one sha256, not fifty-three.** The manifest carries `sourceSha256`, the fingerprint `1qz08ne` and one `packedAt` for the whole source; no table entry carries a hash. A table carries its `description` — *"Boat Module (5).xlsx · sheet “Boat Module”, rows 4–142 under the yellow brand banner…"* — and every row a `Source` cell. A per-table hash would be invented.
3. **Every join hangs off exactly one boat table**, named by its own `Boat →` reference column: 5 on Highfield, Stabicraft, Surtees and Jeanneau; 3 on Stacer and Formosa; 2 on Haines. The far side is the join's second reference — `Motor → Yamaha Outboards`, `Trailer → NSM Custom Trailers`, `Dealer Fit Package →`, `Part →` — and the eight motor joins carry a third, `Rigging Kit Option → Rigging Kits`. **Seven base tables are named by no join:** REDCO, Dunbier, Mackay, ePropulsion, Labour Rates, Oils & Consumables, Registration Costs.
4. **The seed's grouping is nine places, and place → brand → pairing is three levels.** `NORTHSIDE_MODULES`: Boats (7 tables, 810 rows) · Motors (2, 241) · Factory Packages (2, 89) · Trailers (8, 444) · Parts & Accessories (2, 3,587) · Dealer Fit Packages (1, 1,777) · Labour Rates (18) · Oils & Consumables (27) · Registration Costs (19); base 7,012, joins 8,679. `docs/PLAN.md` M2 sets "never more than two disclosure levels" (NN/g, via `docs/reference/dense-tables-and-selection.md`), so one level is a band or a page, never a third chevron.
5. **What a row can honestly carry:** a name; a row count; the kind's label and accent (`TABLE_KINDS`: Boats blue · Motors carmine · Trailers ochre · Accessories viridian · Packages violet · Custom graphite — the honest home for "a bit more colour usage"); heading counts (Stacer 22 series · 91 models; Highfield 7 · 67 · 588 variants; Parts 187 categories; Formosa, Labour and Oils flat); declared price levels (Cash · Trade · Warranty on boats; none on packages, fit, rigging or rates); `retired` on OBSOLETE Trailers and its Surtees join; the workbook sentence. No picture, no date of its own.
6. **Marine has nothing to show.** Partzilla 404s, Boats.net is a Cloudflare wall, Refero a login wall; both Finder guide ids were guesses and landed on the guide's cover, so **Finder's column view is not evidenced**. The earlier run left no frame.

## 1. What is genuinely best — the five hard questions

**1 · A base with five pairings under it: tree, card, column or page?** A column, with the pairings as rows on the page, never a third chevron. `tree/github-repo-tree.png` is the shape: a *Files* pane at 32 px a row with the current one tinted and barred, the object on the right, a one-line provenance strip over it (`0927d3e · yesterday · History`, `348 lines · 15.7 KB`). `live/baserow-template-live.png` is the same shape in a database: *Projects · Tasks · Clients · Team* as a short indented list left of the grid, *1 Sort* and *1 hidden field* as tinted chips, **`8 rows` at the foot**. `live/hf-datasets-glue.png` shows the child set as a counted chooser — *Subset (12) · ax · 1.1k rows ▾* beside *Split (1) · test · 1.1k rows ▾* — with the table under them and *Number of rows 1,485,043 · Total file size 162 MB* at the right. The failure is `live/snowflake-databases.png`: a three-level tree with the kind words (*Tables · Views · Stages*) as group heads and names cut at the pane edge.

**2 · A row whose only facts are a name, a count, a kind and a workbook.** `tree/hf-model-tree.png` is the best register here: `main ▾ · gpt2 · 5.63 GB` and `History: 26 commits` in the head, thirteen rows in 500 px, each glyph · name · a *Safe* chip · size right-aligned · a storage chip · the last-change sentence in grey mono · age. `live/grist-raw-data-scrolled.png` is the same row for tables by name: `ALL Contact Information`, `TABLE ID: ALL_Contact_Information` in small caps beneath, `ROWS: 1,469` at the right. `tree/github-repo-root.png` gives the row its sentence: folder · the last commit's message in grey · age, fourteen rows in 590 px, the head carrying `2704 Branches · 4057 Tags · 35,750 Commits`. Ours: accent tick · name · headings grey (*22 series · 91 models*) · rows tabular right · the workbook sentence · the kind word; `docs/reference/helmlogic-original.md` §4 supplies column-header help for the one fact a dealer may not know.

**3 · A join says what it joins without the word.** By its far side's name, under the table that owns it. The file already names every join *"Stacer × Yamaha — Motor Fitment"*; under Stacer it needs only *with Yamaha Outboards · 434 pairings · motor fitment*, the way `live/hf-datasets-glue.png`'s subset states its parent by position and `prov/dockerhub-tags.png`'s tag card lists its digests beneath it with `+5 more…`. `live/airtable-interface-designer.png` shows the linked thing as a card inside the record, with STATUS · LAST MILESTONE and *Add a record* — a pairing on a boat's page. *Pairings* is the file's own noun.

**4 · Where a sha256 and a workbook name go.** On the table's own page as a labelled row; in the register as one grey sentence. `live2/pypi-details-open.png` is the model: *SHA-256 checksum* as a label with *How to use checksums* beneath it, the hash in mono in a bordered box with a copy control, then a section headed **Provenance** — *"Provenance describes where a file came from"* — and a card: *"PyPI verified that this artifact, at this checksum, originated from the publisher listed below."* `prov/hf-lfs-file.png` does it on a file page: the acts, `548 MB`, a plain sentence, then `SHA256: 248dfc39…` in mono with one line saying what it is. `prov/github-commits.png` keeps the short id at the row's far right with a copy control under a day head; `prov/github-release-assets.png` folds provenance into one line of glyphed facts — *released this last week · 635 commits to main since · 7debcd0 ✓*. So: `1qz08ne` once in the head; the workbook sentence as each row's grey line; the full hash only on the file's page, labelled, with a copy control and a sentence saying what it is for.

**5 · At 390.** `hand/github-repo-390.png` and `hand/hf-tree-390.png` both **drop the sentence column** and keep name · size · age at 41 px. `hand/baserow-template-390.png` folds the table list behind `»` and pins the name column while the grid scrolls sideways, `8 rows` still at the foot. The failure is `hand/hf-glue-390.png`: six phone rows of chips before the tabs. The phone form is its own composition: the place band sticky, rows of name · count, the page full-screen with Back named for the place.

## 2. The patterns worth taking, named

**The whole's size in the head.** `tree/hf-model-tree.png` (`5.63 GB`), `tree/github-repo-root.png` (`35,750 Commits`), `live/baserow-template-live.png` (`8 rows` at the foot). Ours: *53 tables · 15,691 rows*, counted off the store.

**The counted chooser.** `live/hf-datasets-glue.png`: *Subset (12) · ax · 1.1k rows* — how many and how big, before it is opened.

**The provenance strip over the object.** `tree/github-repo-tree.png`, `prov/github-release-assets.png`: one line of glyphed facts between the name and the thing.

**The hash as a labelled row with a copy control and a sentence.** `live2/pypi-details-open.png`, `prov/hf-lfs-file.png`. Never a column.

**The list beside the thing, the current row barred.** `tree/github-repo-tree.png`, `live/baserow-template-live.png`; Airtable's sidesheet in `dense-tables-and-selection.md`.

**The description as the second line, the path as the breadcrumb.** `live2/metabase-browse-scrolled.png`: *Accounts · Sample Database › PUBLIC · Created 19 days ago* over *"Information on customer accounts registered with Piespace…"* — the seed's own `desc` lines, written to carry no count.

**A named place for what is no longer sold.** `live2/linear-projects-scrolled.png`: deleted projects sit "under the *Recently deleted projects* tab for 30 days". OBSOLETE Trailers is a band that says *no longer sold*, as `censusLine` already prints.

## 3. Type and motion, as observed

One size and two greys: GitHub sets the name full and the sentence grey at one size; Hugging Face sets sentences, sizes and hashes in mono and the name in the body face; Docker sets the digest in mono as the link itself; Grist labels in small caps (`TABLE ID`, `ROWS`) over body-weight values. Counts are right-aligned in the body face in every frame and never coloured; the one colour in a register row is Docker's vulnerability bar, which is a message, not a tint. Row pitch measured: 32 (GitHub tree), 40 (Hugging Face), 41 (GitHub root), ~33 (Baserow). Motion: none proven in 58 stills; a register of tables changes once per import, so nothing on it moves but the row under the pointer. `dense-tables-and-selection.md` carries RAIL's 100 ms and the tap-versus-hold peek.

## 4. What to avoid, each with its frame

- **Three levels with kind words as heads** — `live/snowflake-databases.png`.
- **The type vocabulary at the door** — `live/supabase-tables-scrolled.png` (INTEGER · TEXT · JSON in caps under columns), `customers/empty/notion-create-database.png` ("select a database type").
- **The chip wall** — `hand/hf-glue-390.png`; and `home/live2/github-repos-register.png`, where topic chips, a description and a sparkline leave **five rows at 1440 × 900**.
- **A label printed with no value** — `live/kaggle-iris.png`: *Expected update frequency: Not specified*.
- **The one act on a row being the destructive one** — `live/grist-raw-data.png`: `…` opens *Remove*, and the page warns it "*will* delete data and remove it from all pages". Ours: undo, never a warning.
- **A scaled shot standing for a register** — `live/tableplus.png`: anatomy visible, size unreadable.

## 5. Four directions

Each a different composition **and** a different grouping. Every board owes the head strip (*53 tables · 15,691 rows · read from the file · `1qz08ne`*), the workbook sentence per table, the page with the labelled hash, and 18 rows at 1280 × 800 (604 px of room, the quotes sweep's arithmetic, at 28 px).

**A — "The brand and its pairings".** A column view: the 25 base tables as one list on the left under nine place heads that are bands, not chevrons — accent tick · name · rows — and on the right the chosen table's page: its mark or its name in type, the workbook sentence, the heading counts, then its pairings as rows (*with Yamaha Outboards · 434 pairings · motor fitment*), each opening that pairing's sheet. Grouped by place, then table; the join lives only on the page, so two levels. **Exclusive to A:** `tree/github-repo-tree.png` and `live/hf-datasets-glue.png`. **Only A shows a table and what pairs with it in one window.** Reflow: 1920 — list 360, page 1,000; 834 — list 280 beside the page; 390 — the list is the screen, a row opens the page full-screen, Back named for the place. Second dealership: the marks; a table with no mark draws its name on the kind's accent plate, as eleven of our 25 already must.

**B — "The ledger".** One full-width list, everything visible, no pane and no chevron: nine place bands each carrying its table count and row sum, every base table a row, every pairing an indented lighter row beneath its boat (`× Yamaha Outboards · 2,519 · motor fitment`). A row is accent tick · name · headings grey · rows tabular right · workbook sentence · kind word, at 28 px: Boats' band, four brands and sixteen pairings fill the first screen at 1280 × 800 — 20 rows. Grouped by place, then table, then pairing, in the seed's order. **Exclusive to B:** `tree/hf-model-tree.png` and `tree/github-repo-root.png`. **Only B puts all 53 on one scroll**, so only B answers "what does the file hold" without a press. Reflow: 1920 — the same ledger on a 1,040 measure; 834 — the sentence column drops, GitHub's own 390 rule; 390 — name · rows only, bands sticky. Second dealership: only words and counts change.

**C — "The showpiece row".** Marks first: the seven boat brands as a row of plates across the top, each the mark large with its rows count and a strip of pairing chips beneath (*Yamaha 2,519 · NSM Custom 146 · GFAB 51 · Dealer Fit 1,342 · Parts 1,707*), and every other table as plain rows under place heads below. Grouped by **what pairs**: motors, trailers, fit and parts appear once as rows and again as chips on the boats they fit. **Exclusive to C:** `prov/dockerhub-tags.png` (the mark large at the head, its rows beneath, `+5 more…`) and `live/airtable-interface-designer.png` (the linked thing as a card inside the record). **Only C makes the logo the showpiece**, and only C must draw Stabicraft, REDCO and NSM Custom without one. Reflow: 1920 — seven plates in one row; 834 — four and three; 390 — one plate per row, chips wrapping, the rest as rows. Second dealership: every plate is theirs; with no marks the plates are names on accent, which the board draws.

**D — "The workbooks".** Grouped by **where it came from**: seven bands on a spine — *Boat Module (5).xlsx* (7 brands and all 28 pairings), *Trailer Module.xlsx* (8), *Motor Module* (4), *Parts Module (3).xlsx* (2), *Rigging Module.xlsx*, *Service Module (1).xlsx* (2), *Registration Module.xlsx* — each row a table with its sheet and row range where the sentence would be, the fingerprint at the head, the hash on the page. Ordered by workbook, then by row range. **Exclusive to D:** `prov/github-commits.png` (rows under a day head on a spine, the short id far right with a copy control) and `live2/pypi-details-open.png` (the checksum as a labelled row, provenance as a sentence). **Only D is ordered the way the file is maintained**, which re-importing (M4) needs; drawn so the owner can reject it knowingly — it wins provenance and loses "Stacer with its pairings under it". Reflow: 1920 and 834 — the spine; 390 — bands sticky, rows name · rows. Second dealership: a file from one workbook is one band with a sentence saying so.

## 6. The imagery question

**This screen needs none of it.** Measured on disk 2026-09-22: `images.json` 453 addresses · 329 held · 118 unheld · 6 refused, every held copy at long edge ≤ 1,100; `heroes-ledger.json` eight on-water photographs, four Highfield at 2560 and four Stacer at 1200–2560; `marks-ledger.json` 18 rows · 13 brands · 17 files, Stabicraft *"no public wordmark verified"*, Mercury white-ink only. A picture belongs to the exact model it depicts; a table depicts 91 models, so the only honest picture for the Stacer row is its mark — and a mark exists for 14 of 25 base tables and 0 of 28 pairings (REDCO / Tinka, NSM Custom and OBSOLETE have none; parts, fit, rigging and the three rate registers are not brands). A direction leading rows with marks draws the other 39 first. The kind's accent is the one visual every row has. A second dealership replaces the marks and nothing else changes.
