# Screens

A living inventory. One row per screen: its register, the one thing it does that no other screen does, the references its board cited, the direction doc, and where it stands. A screen is "done" only after the owner has looked at it in the browser against the picked direction.

| screen | register | what only this screen does | primary references | direction doc | status |
|---|---|---|---|---|---|
| Entry (`/sign-in`) | Showroom | offers the file — the one moment the Master Price File is a thing you choose to open, with the photograph's own row named beside it — and says who is at the desk, honestly not authentication until M6 | Riviera ownership (the pennant off the top edge) · Princess (one pill low on chosen-dark water) · Zodiac (the caption in the picture's quiet quadrant) · Lucid & Garmin (a card floated on a photograph) · Slack (the instruction as the headline) · StackBlitz & Cloudflare (a named step that ticks, a figure that is a dash until it is known) · plus Porsche ID · BMW OneID · Polestar · Tesla · Linear | `directions/entry/` (four boards, B recommended) | **built, PROVISIONAL** — `src/screens/entry/` from board B “Veil and card”, built while the owner was away under the plan's standing rule; the first independent critique (`directions/built-critique.md`) was answered on 2026-09-17 — the caption's contrast, the phone photograph, the missing ledger id, the 1920 seam and the busy state — and the owner has not looked |
| Home (`/`) | Showroom | greets the dealer with what they sell, photographed, and one act: New quote | Nimbus builder · Porsche Finder · Saxdor · Axopar · Highfield range · Stabicraft · Zodiac · Sea Ray · Grady-White · Whaler · Riviera · Rapha (two photographs as one fold) · Sotheby’s (the caption on its own ground) · Hagerty (the split record card) | `directions/home/` (four boards, B recommended) | **built, PROVISIONAL** — `src/screens/home/` from board B “Cinema day”, built while the owner was away under the plan's standing rule; both independent critiques answered in full — the design round of 2026-09-17 (`directions/built.md` § “Home, after the critique”) and the flow round of 2026-09-18, which found the one act on the screen dead and its reason false (§ “Home, after the second critique”: the act, both plates, the register and a filed document all reach the screens they name, and the three sentences about unbuilt screens are deleted); the owner has not looked |
| Picker (`/quote/new`) | Showroom | shows what a model IS in the price file — how many rows, at how many figures, in which materials and which colourway codes — and turns one of those rows into a document | Porsche model overview (the counted rail, "All (72)") · Williams tenders (rail, column and fact panel live in one window) · Surtees 770 (the three-fact strip with the act at its right end) · Grady-White (LENGTH · BEAM · MAX HP) · Bang & Olufsen (a finish is a tile with its own figure, and the count written not swatched) · Nimbus builder (the sentence where the price would be, act still live) · Framework marketplace (a count on every row) · Saxdor & De Antonio (a chooser made of type alone) | `research/refs/picker/notes.md` §5, direction **C** | **built, PROVISIONAL** — `src/screens/picker/` from direction C “Three tiers at once”, chosen by the builder because the owner handed the picks over; the owner has not looked |
| Configurator (`/quote/$id`) | Showroom | one chapter per decision with a live price, a search above them that reaches every chapter and every one's shortlist at once, and every refusal shown with its reason on a row that stays live — and it is the only screen in this app where a press changes a document | Porsche search-open (a field above the chapters turning the panel into grouped, priced, tickable rows with no mode change) · Porsche 911 rail (four row shapes for four kinds of content) · Porsche summary (the dealer's own code in grey beside every name; `Standard Equipment` as a word where the figure would be) · PCPartPicker (a build as a priced table, an em-dash in every empty cell, and a warning banner that disables not one row) · Boston Whaler engines (the delta against what is fitted, with the minus printed, while the stage keeps the running figure) · Herman Miller Aeron (the numbered head naming its own value while closed) · Framework marketplace (a count on every category) · Apple MacBook Pro (the refusal printed on the candidate before it fires) · GOV.UK error summary (the problem named in words, linked to the control that caused it) | `research/refs/configurator/notes.md` §5, direction **B** | **built, PROVISIONAL** — `src/screens/configurator/` from direction B “Stage and rail”, chosen by the builder because the owner handed the picks over; the second independent critique (`directions/built-critique.md`, 2026-09-17) was answered on 2026-09-18 — the 2.99:1 labels, the finale that stranded a dealer after issuing, the stage that had no stage, the silent second motor, the refusal printed once per row, the 3.31px sticky slit and the star the sweep said not to draw — and the owner has not looked |
| Cascade (`/quote/$id/cascade`) | Showroom | shows what comes on, what goes, what stays and why, priced as a decision the dealer accepts or declines — and it is the only screen in this app that prices a change BEFORE it is made, grouped by cause rather than by verb, with every reason the engine's own | Porsche's feasibility sheet (the routed address, the blurred stage, the footer that prices the decision and not the click, `Standard Equipment` where `$0.00` would go) · Terraform plan (the verb sentence with the `because` on its own line, and the census) · HCP Terraform confirm (the census restated above the act) · Wikipedia diff (what stays, shown by not moving, with the reason on the side that caused it) · Helm diff (a heading that is a SENTENCE naming the object and its fate, before the object) · Google Docs suggestions (crossed out where it dies, and accepted rather than announced) · PCPartPicker incompatible (the severity in words, and not one control disabled) · Boston Whaler engines (the alternative priced as a difference, downward with the minus printed) · Stripe prorations (two lines whose NAMES are the reason, running to the difference) · Vercel instant rollback (the consequences listed before the act, and a section on undoing it) | `research/refs/cascade/notes.md` §6, direction **B** | **built, PROVISIONAL** — `src/screens/cascade/` from direction B “Because”, chosen by the builder because the owner handed the picks over; the owner has not looked |
| Document (`/quote/$id/document`) | Showroom / paper | the issued quote as ONE OBJECT: a sheet of A4 at true size on the floor, printed from the same nodes with `@page`, so there is no second renderer for screen and paper to disagree through — the only screen in this app whose output is a physical thing, the only one that renders with no catalogue at all, and the only one that decides its own page breaks | Porsche's configuration PDF, page by page (the cover's order; `Standard Equipment` where a figure would be; the code repeated on every page) · paged.js (crop marks and a page box as the page's own furniture) · Saxdor's 320 GTC A4 brochure and its `LAYOUTS & SPECS` (white sheets on a grey floor, hairline spec rows at ~55% measure, a lower third left white) · GOV.UK check-answers, summary-list and confirmation ('Not provided'; the reference in a block; "a way for users to save a record… as a PDF") · Qwilr (the band head carrying its own subtotal) · Stripe billing (the arithmetic printed under the line's name) · Butterick on tables and page margins (borders off first; "one inch is not enough") | `research/refs/document/notes.md` §5, direction **A** | **built, PROVISIONAL** — `src/screens/document/` from direction A “The sheet, at true size”, chosen by the builder because the owner handed the picks over; the owner has not looked |
| Quotes register (`/quotes`) | Cockpit | every draft and issued quote as one dense ledger whose STRUCTURE is the state — three bands, no state column, no coloured pill — found by reference, customer, boat or who prepared it, read without leaving the list, and versioned through the supersedes link the contract already carries | GitHub issues & pulls (the row's weight: a glyph, a title, one quiet line) · Linear filters (the group header that IS the state, with its `n / m` fraction) · Linear issue status (the glyph's shape carries the stage) · Linear Peek (tap Space to peek, hold to glance) · Mercury (a right-aligned balance on a dark register, and the act as the last row) · Shopify order statuses (superseded as an additional status explained in a sentence) · Stripe search (the published one-field grammar) · MarineMax (a sentence where the price stands) · GitHub releases (the version rail with one Latest) · Primer blankslate & Telwater's WHAT HAPPENED / WHAT DO I DO (the first-run empty state) | `research/refs/quotes/notes.md` §6 (four directions, A chosen) | **built, PROVISIONAL** — `src/screens/quotes/` from direction A “The ledger”, chosen by the builder under the owner's handover; the flow critique of 2026-09-18 answered in full (its blocker was that the register could not open what it listed — the seam was never cut; `openQuote(id, state)` and `newQuote` are now props, a draft opens at `/quote/$id` and an issued one at `/quote/$id/document`, and the two false sentences are deleted), and the ledger no longer stretches its frame to the window; the owner has not looked |
| Lost (any address with no screen) | Foundation | draws an ADDRESS the app does not have, as the subject — set large in the mono face, labelled, cut at 120 characters with the cut said out loud — and is the only screen whose every control is a real link; it is also where a screen that threw lands, quoted word for word, with a way to draw it again | GOV.UK page-not-found (the address named, then the routes onward, never an apology that says nothing) · the app itself: entry B’s door bar as a primitive SIZE and home B’s single amber act as a primitive INTENT, and nothing else borrowed — no photograph, no veil, no card | none: built against `docs/directions/built-critique.md` § app-wide rather than a sweep, because the finding is that this surface did not exist at all | **built, PROVISIONAL** — `src/screens/lost/` wired at `src/routes/__root.tsx` (`notFoundComponent`) and `src/app/router.ts` (`defaultErrorComponent`); joined `e2e/routes.ts` as `/nope`, and contrast, cut and overlap are green on it at all six viewports; the owner has not looked |

Milestone 2 onward (customers, data, sheet, history, rules, fitment, review, levels, places, organisation, people, import/export, templates, whiteboard, pipeline, admin) are added here when their sweeps begin.

Rules for this table: no two rows share a primary reference set; "same treatment as X" never appears in a direction doc; the frames a board cites live under `research/refs/<screen>/` with their source URLs in `sources.json`.

## How the two built screens join (the shell, 2026-09-17)

There is no shell component and there is not going to be one: what joins two screens is the router, the session store and the catalogue store, and all three read in one sitting.

- **The first-visit rule.** A browser with no name in the session lands on Entry; a browser with one lands on Home. Both halves are `beforeLoad` — `src/routes/index.tsx` and `src/routes/sign-in.tsx` — so the rule runs on a typed address, a Back, a refresh and a preload alike, and no screen is ever painted and then dismissed. It is not authentication (`src/state/session.ts` says why at length); it is the order of two screens.
- **Entry reads the file. Home reads this browser.** Entry's blue door is the only caller of `openCatalogue`, the one function that can fetch the pack. Home calls `catalogue.load(repository)`, which cannot fetch. So the blank door really does leave Home with nothing counted, and a second visit really does read the sheet back out of IndexedDB — measured at 315 ms on a desk and 392 ms in a hand, with no pack file fetched.
- **What crosses the navigation.** The catalogue store carries `from` (`'pack'` or `'repository'`) and `business` (the manifest's name, or the name filed beside the sheet), both derived from whichever source was loaded. That is how Home's stamp says `read from the file` for work Entry did, and `read from this browser` for work it did itself.
- **The way back.** A desk with no price file says so and offers the door: Home's *Load the Master Price File* goes to `/sign-in?again=true`, the one address that reaches Entry with a name already given, and the field starts with the remembered name.
- **Every browser-side test walks it.** `e2e/routes.ts` gives each route an `arrive`, and `open()` in `e2e/shots/recipe.ts` honours it, so all five rulers and the shot recipe reach Home the way a person does.

## Cross-cutting, every screen from here

Read `docs/CUSTOMISATION.md` before drawing a board. Every direction must answer two questions on the board itself:

1. **Which parts of this composition can the dealer replace** — the photograph, the mark, the accent colour, the density — and where do they do it?
2. **What does this screen look like once they have?** A direction that only works with the picture it was drawn on breaks the first time it meets a second dealership. Draw the second state, or name the bound that keeps the first one true.

Nothing on a screen hard-codes a colour, a face or a picture path. Every value is a token, so an organisation's own layer overrides it at boot without a rebuild.

Every board also says **how it reflows**: what the screen becomes at 390px in a hand, at 834px on a tablet beside a hull, and at 1920px in the office. The rulers run at six viewports and a layout that only holds where it was drawn fails the gate.
| History (`/history`) | Cockpit | shows what was DONE, event by event, on the day it was done: one spine, the days as nodes on it, one folded line per quote touched that day counting its kinds in words (`started · 7 picks · issued`), opening in place to the commands' own sentences with the time each was said — the only screen that reads the per-document diary at all, and the only one where the DAY is the band and what happened is the title; `Quote this again, at today's prices` is the act on a past thing, with `whyNotAgain`'s refusal where it stands, and the new document lands under Today as the diary records the act it just did | GitHub PR #20463 conversation (`eps1lon added 21 commits` — same-kind events counted into one line on the spine, and the chain read as a sentence with counts, never a diagram) · Sublime Merge (message over author with the time set right: the three tones of an opened entry) · Stripe payouts (the return reason read on the timeline — the refusal where the act is) · Netlify deploys (`Last hour · Last day · Last 7 days` as controls, and `Publish deploy` on the past deploy's own page, where "quote this again" stands) · Figma & 1Password restore (the act on the version itself, and the one word this screen must print that they do not: it is NOT a restore) · Things (a finished day as a tally — a content claim, no pixel) · GitHub commits in a hand (the 390 reflow only: the spine at the gutter, the meta under the title) | `research/refs/history/notes.md` §5, direction **A** ASSIGNED by the round (`critique-m2.md` §3: five of twenty directions were list-left, detail-right), with B's gutter tally kept as the day head's figure and C's one-quote diary kept as what a line opens INTO | **built, PROVISIONAL** — `src/screens/history/` from direction A "The spine", built under the owner's handover; the engine hole the critic found (`groupByDay` cutting by `createdAt`, no reader over `QuoteEvent.at`) is closed by `src/domain/quote/diary/days.ts` with the two-calendars and midnight cases pinned, and the store now keeps the event that made a document ON the document (`src/state/quotes.ts`, `file`), without which every freshly minted quote had an empty diary; the fold is owned as an invention in `history.css`; measured at 1280×800 with the walk's one document: 691px of room less a 61px Today node holds 22 lines at a 28px pitch, 20 with two more day nodes; the owner has not looked |

## Milestone 2 rows, appended as each screen is built

| screen | register | what only this screen does | primary references | direction doc | status |
|---|---|---|---|---|---|
| The sheet (`/data/$table`) | Cockpit | one table of the price file, dense and worked in and edited in place: the file's own order as one grid, the hierarchy as bands with a count on every one and a depth ladder in the file's own column names, identity pinned, sections folding to chips, every cell a button that opens on Enter with the engine's own refusal as a pill beneath it, every write one command with Undo pinned to it under the head — and the only screen where a dealer's cost column is drawn, marked "cost" at its head. The gallery is the second door (the unit is the model, one card per drawer under its series, the held render or the maker's mark), the record the third (the row as a spec sheet under the table's own sections, every value a button, the row lit behind it, the screen itself in a hand); a pairing is filed under its boats | Excel's outline (`sheet/tools/excel-outline.png`: one press opens the sheet to a level; a group's summary as a row of the grid) · AG Grid row grouping (`sheet/tools/aggrid-grouping.png`: the count on the band, the group column pinned, levels as chips) · NocoDB expanded record (`sheet/tools/nocodb-expand.png`: the side panel with the row lit, a full-screen drawer in a hand) · NocoDB gallery (`sheet/tools/nocodb-gallery.png`: a plate of cards over two lines of facts) · Figma's mixed selection (`sheet/tools/figma-mixed.png`: each value listed once with its count before a fill) · Airtable row height (`sheet/tools/airtable-grid-rowheight.png`: the picture a row can carry at three heights) · Grist enter-data (`sheet/tools/grist-enter-data.png`: the paste contract said aloud; Space toggles, Enter opens) · Highfield Sport 560 specifications (`sheet/boats/highfield-sport-560-specs.png`: a render per colourway captioned with the very codes the Variant column holds; label over value) | `research/refs/sheet/notes.md` §5, direction **A** assigned, with B's gallery and C's record kept as the two other doors and D's boat-side master for a pairing; §7 reconciles the published table numbers with the engine | **built, PROVISIONAL** — `src/screens/sheet/` at `src/routes/data.$table.tsx`, built under the owner's handover and the critic's assignment; joined `e2e/routes.ts` as `cockpit` on Highfield Inflatables, the file's worst table, and the density ruler read **18 records in view · 28px pitch · 655px room less 168px of band heads → holds 17** at 1280×800; contrast, cut, overlap and the ramp green at six viewports; the owner has not looked |
